{   
    "monitoring_groups": {
        "description": "监控群",
        "type": "list",
        "hint": "启用人机禁言的群聊， 可用命令 “人机禁言 开/关” 单独为当前群聊设置 ",
        "default": []
    },
    "max_length": {
        "description": "消息最大长度限制",
        "type": "int",
        "hint": "发言规则一：人机发言超过此长度时会被禁言（单位为字）",
        "default": 150
    },
    "speak_threshold": {
        "description": "发言间隔时间限制",
        "type": "int",
        "hint": "发言规则二：人机前后两次发言间隔小于此值时会被禁言（单位为秒）",
        "default": 20
    },
    "is_delete_msg": {
        "description": "是否撤回消息",
        "type": "bool",
        "hint": "是否在禁言人机的同时，撤回人机发的消息",
        "default": false
    },
    "ban_duration": {
        "description": "禁言时长",
        "type": "int",
        "hint": "人机发言违规时被禁言的时长（单位为秒）",
        "default": 1800
    },
    "ban_sleep": {
        "description": "通融时间",
        "type": "int",
        "hint": "当机器人是被@唤醒时，人机检测程序会睡眠一段时间，给人机发言的机会（单位为秒）",
        "default": 3
    }
}